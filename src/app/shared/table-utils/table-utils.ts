import { MatDialogConfig } from '@angular/material';

export const TableDialogConfig = <MatDialogConfig> {
    disableClose: true,
    position: {
      top: '4px',
      right: '4px',
    },
    height: '99vh'
}