import { NavStyleChangeDirective } from './nav-style-change.directive';

describe('NavStyleChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new NavStyleChangeDirective();
    expect(directive).toBeTruthy();
  });
});
